<Window x:Class="FaceRecognitive.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FaceRecognitive.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        
        <Image Name="image" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox Name="NameTextBox" Width="150"/>
            <Button Content="Train" Name="Input" Click="Input_Click" Padding="5"/>
            <Button Content="Recognize" Name="Recognize" Click="Recognize_Click" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
